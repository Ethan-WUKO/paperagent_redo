# RAG 综合评测样本文档

文档编号：`KB-EVAL-BLUEMIST-2026-07`

唯一检索标记：`kb-orchid-314159`

最后更新：`2026-07-05`

## 1. 基本概况

本文件用于模拟一个真实但虚构的项目知识库条目，方便测试 RAG 系统在以下场景中的表现：

- 精确事实检索
- 别名与简称识别
- 表格内容提取
- 历史版本与当前版本区分
- 跨段信息拼接
- 对“文档中未提及信息”的谨慎回答

项目正式名称是“青岚知识助手”。

项目常用简称是“青岚助手”。

项目英文名称是 `BlueMist Assistant`。

项目内部缩写是 `BMA`。

需要注意：`Project Cedar` 是 2026 年 5 月之前使用过的旧代号，已经停用，当前不再作为正式名称使用。

## 2. 关键人员与别名

导师姓名：林知远

项目负责人：周雨桐

项目负责人的英文名：Tina Zhou

后端负责人：王晨

前端负责人：王辰

数据标注协调人：陈可

测试联络人：赵一帆

有两个名字读音相近但不是同一个人：

- 王晨：后端负责人
- 王辰：前端负责人

在周会纪要里，如果只写“小王”，默认指的是“王晨”。

只有在明确写出“前端小王”时，才指“王辰”。

## 3. 当前有效的项目配置

下表中的信息是当前有效配置，不要和历史记录混淆。

| 配置项 | 当前值 |
|---|---|
| 默认对话模型 | `DeepSeek-V3.1` |
| Embedding 模型 | `text-embedding-v4` |
| 重排模型 | `bge-reranker-v2-m3` |
| 检索 Top K | `12` |
| Chunk 大小 | `700` 字符 |
| Chunk Overlap | `120` 字符 |
| 默认 `max_steps` | `18` |
| `max_steps` 可配置范围 | `5` 到 `60` |
| 优先支持的文件类型 | `PDF`、`DOCX`、`Markdown` |
| 默认语言 | 中文 |

补充说明：

1. 当前版本默认优先上传 `PDF`、`DOCX` 和 `Markdown` 三类文件。
2. 如果用户没有单独修改设置，系统默认 `max_steps` 为 `18`。
3. 如果用户把 `max_steps` 设置到 `60` 以上，应视为超出当前允许范围。

## 4. 时间线与里程碑

当前有效的周会时间是：每周二 `16:10`。

采购冻结日期是：`2026-08-12`。

论文材料提交冻结日期是：`2026-08-18`。

安全评审时间是：`2026-08-20 14:00`。

对外演示时间是：`2026-08-26 09:30`。

对外演示地点是：`B-402` 会议室。

里程碑之间的关系如下：

- 采购冻结早于材料提交冻结。
- 安全评审晚于材料提交冻结。
- 对外演示晚于安全评审。

## 5. 设备与资产清单

| 资产名称 | 所在位置 | 责任人 | 序列号 | 备注 |
|---|---|---|---|---|
| 会议一体机 | `B-402` | 王晨 | `SN-A17-442-KB` | 用于对外演示 |
| OCR 扫描仪 | `A-307` | 陈可 | `OCR-9X-118` | 用于纸质资料数字化 |
| 备用笔记本 | `C-105` | 周雨桐 | `LT-77-AX19` | 演示日备用设备 |
| 路由器备机 | `B-401` | 赵一帆 | `NET-22-QA7` | 网络故障时启用 |

如果有人问“B-402 会议室的核心设备归谁负责”，答案是王晨，因为会议一体机位于 `B-402` 且责任人是王晨。

## 6. 操作规则

知识库上传文件推荐命名格式为：`YYYYMMDD_topic_owner_vN`。

红色级别故障需要在 `30` 分钟内上报给周雨桐。

如果是前端页面样式异常，应优先联系王辰。

如果是索引、检索、接口或演示设备问题，应优先联系王晨。

如果问题涉及纸质资料扫描失败，应联系陈可检查 `A-307` 的 OCR 扫描仪。

## 7. 论文与资料流程说明

当前论文资料处理流程共有五步：

1. 收集原始材料
2. 结构整理
3. 摘要与要点提取
4. 章节润色
5. `OpenAlex literature recommendation export`

如果有人问“论文资料流程的最后一步是什么”，正确答案是 `OpenAlex literature recommendation export`。

## 8. 常见问答

问：项目正式名称是什么？

答：青岚知识助手。

问：`BlueMist Assistant` 指的是哪个项目？

答：它就是“青岚知识助手”的英文名称。

问：`Project Cedar` 是当前项目名吗？

答：不是。它是旧代号，已经停用。

问：导师手机号是多少？

答：本文件没有记录导师手机号。

问：实验室门禁密码是多少？

答：本文件没有记录门禁密码。

问：如果周会纪要里只提到“小王”，一般指谁？

答：默认指王晨。

## 9. 历史记录

以下内容是历史信息，只能用于回答“过去是什么”，不能覆盖当前配置。

### 9.1 旧周会时间

在 `2026-06-15` 之前，周会时间曾经是每周一 `10:00`。

从 `2026-06-15` 开始，周会改为每周二 `16:10`。

### 9.2 旧模型配置

在 `2026-06-28` 之前，默认对话模型曾经是 `GLM-4.5`。

从 `2026-06-28` 开始，默认对话模型改为 `DeepSeek-V3.1`。

### 9.3 旧项目代号

在 `2026-05-20` 之前，内部有人把项目称为 `Project Cedar`。

从 `2026-05-20` 起，统一改为 `BlueMist Assistant` 或“青岚知识助手”。

## 10. 交叉信息提示

对外演示地点是 `B-402`。

`B-402` 里的会议一体机责任人是王晨。

因此，如果要在演示开始前检查 `B-402` 的演示设备，应该优先联系王晨。

材料提交冻结日期是 `2026-08-18`，安全评审时间是 `2026-08-20 14:00`。

因此，安全评审发生在材料提交冻结之后。

## 11. 明确未记录的信息

为了测试系统是否会胡乱补全，这里再次强调以下信息没有出现在本文档中：

- 导师手机号
- 实验室门禁密码
- 周雨桐的家庭住址
- OCR 扫描仪采购价格

如果被问到这些内容，合理回答应该是“文档中没有提供”或“无法从当前文档确认”。

## 12. 附加检索锚点

以下字符串仅用于测试精确召回，不代表真实业务含义：

- `kb-orchid-314159`
- `bluemist-anchor-2026`
- `RAG-CHECK-DELTA-88`

如果系统连这些唯一标记都检索不稳定，通常说明索引、切分、召回或 rerank 环节还需要继续排查。
