package com.yanban.core.agent.worker;

/** Coarse material kind used only for structured comparison. */
public enum WorkerMaterialType {
    PAPER,
    BIBLIOGRAPHY,
    CODE,
    CONFIGURATION,
    EXPERIMENT,
    OTHER
}
