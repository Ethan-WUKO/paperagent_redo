package com.yanban.core.research;

/** Closed set of model-result item shapes for the first scientific tool batch. */
public enum ResearchToolItemType {
    LATEX_OUTLINE,
    BIBTEX_AUDIT,
    CODE_SYMBOL,
    EXPERIMENT_SUMMARY,
    CROSS_MATERIAL_LINK,
    LITERAL_MATCH
}
